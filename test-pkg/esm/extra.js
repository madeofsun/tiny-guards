import { Guards } from "tiny-guards";
if (typeof Guards === "undefined") {
  throw new Error("Guards is not defined");
}
import { Guards as Guards$2 } from "tiny-guards/guards";
if (typeof Guards$2 === "undefined") {
  throw new Error("Guards$2 is not defined");
}
